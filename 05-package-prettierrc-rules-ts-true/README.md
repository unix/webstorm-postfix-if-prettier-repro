# 05 Package Plus Prettier Rules

Result: reproduces.

## Steps

1. Open this folder as a WebStorm project.
2. Open `src/repro.ts`.
3. Type `str.if`.
4. Press `Tab`.

## Observed

The generated `if` block contains an unexpected `a`.
